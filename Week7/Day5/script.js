let b = 3, d = b, u = b;

const tree = ++d * d*b * b++ +  /144  //4*4*3*3 
 + --d+ + +b-- + // + 3 + 4   d = 3 b = 3
 + +d*b+ +  // +  3*3
 u  // + 3

console.log(tree); // 163

let res = x > 2000 ? "You are in the 21st century": "you live in the middle age" ;

const result = (a, b, operator) =>
    b == 0  ?  "impossible divide by 0" : 
                        operator == "+" ? a+b :
                            operator == "-"? a-b :
                             operator == "/"? a/b :
                              operator == "*"? a*b : "operator is not *, /, + or -" 

let div = 2;
let atLeng
for (let index = 0; index < array.length; index++) {
    const element = array[index];
    
}      

let arr1 =[] ;

let arr2 = [];

arr1.forEach